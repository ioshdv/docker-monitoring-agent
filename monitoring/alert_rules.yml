groups:
  - name: app_rules
    rules:
      - alert: AppDown
        expr: up{job="my-app"} == 0
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: "App is down"
          description: "Prometheus cannot scrape the /metrics endpoint."
